/*Challenge2 Task 1: Just get familiar with the syntax of the exported function and what the function does*/

/* From circle-area.js... */
export function area(radius) {
    return Math.PI * radius * radius;
  }